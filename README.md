📱 Мій додаток: Менеджер Контактів (Cross-Platform)
Це мій навчальний проєкт на Flutter, створений як універсальне рішення для Android, iOS та Windows. Додаток допомагає зручно керувати контактами, використовуючи сучасні підходи до розробки.

🌟 Що вміє додаток:
Зберігати дані: Використовується локальна база даних. Всі контакти надійно зберігаються на пристрої.

Працювати з фото: Можна додати аватарку прямо з камери телефону або вибрати з галереї (Image Picker API).

Показувати локацію: Реалізовано роботу з GPS (Geolocator API) — на головному екрані в реальному часі відображаються координати.

Сповіщення: Інтегровано базу для Firebase Cloud Messaging (для мобільних платформ).

Зміна теми: Додаток підтримує світлий та темний режими. Я обрала унікальний Bordeaux Style (бордові градієнти та акценти).

Керування жестами: Видалення контактів реалізовано через нативний жест "свайп" (Dismissible).

🏗 Як все влаштовано (Архітектура MVVM):
Я використала архітектуру MVVM та провайдер станів (Provider), щоб код був чистим та структурованим:

Model (User): Опис структури даних користувача.

Database/Services: Логіка збереження даних та роботи з нативними API.

ViewModel (UserViewModel): Тут відбувається вся логіка — обробка даних, завантаження списку та оновлення інтерфейсу без зайвих перезавантажень.

Views (UI): Екрани, побудовані на базі віджетів, що адаптуються під розмір екрана (від смартфона до вікна Windows).

🛠 Нативні функції та API:
Камера та Галерея: image_picker.

Геолокація/GPS: geolocator.

Локальна база даних: sqflite (з підтримкою FFI для Windows).

Push-повідомлення: firebase_messaging (ізольовано для стабільної збірки на десктопі).

⚙️ Як запустити:
Завантажити код із репозиторію.

У терміналі виконати: flutter pub get.

Для Windows: Переконайтеся, що активовано Developer Mode, і пропишіть flutter run -d windows.

Для Android/iOS: Підключіть пристрій та натисніть Run.
