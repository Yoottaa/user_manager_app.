# Мій додаток: Менеджер Контактів 📱

Це мій навчальний проєкт на Flutter. Додаток допомагає зберігати список людей, їхні номери телефонів, пошту та фотографії.

### Що вміє додаток:
* **Зберігати дані**: Всі контакти не зникають після закриття програми (використовувала `shared_preferences`).
* **Працювати з фото**: Можна додати аватарку прямо з камери телефону або вибрати з галереї.
* **Показувати локацію**: На головному екрані зверху виводяться координати, де зараз знаходиться телефон.
* **Сповіщення**: Додана база для роботи з Push-повідомленнями через Firebase.
* **Зміна теми**: Є світлий та темний режими (кольори підбирала сама — бордовий стиль).

### Як все влаштовано (Архітектура):
Я розділила код на частини, щоб не було каші в одному файлі:
1. **User**: Опис того, які дані є у користувача.
2. **DatabaseHelper**: Логіка збереження списку контактів у пам'ять телефону.
3. **UserViewModel**: Тут відбувається вся "магія" — завантаження списку, видалення та оновлення екрана.
4. **Екрани (UI)**: Головний список та окрема форма для додавання/редагування.

### Нативні функції, які я використала:
* **Камера та Галерея** (пакет `image_picker`).
* **Геолокація/GPS** (пакет `geolocator`).
* **Локальне сховище** (пакет `shared_preferences`).
* **Firebase** (для повідомлень).

### Як запустити:
1. Завантажити код.
2. Прописати в терміналі `flutter pub get`.
3. Підключити телефон або запустити емулятор.
4. Натиснути Run.
